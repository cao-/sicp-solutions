\begtt\scm
(define (sum-squares-larger-two x y z)
   (sum-of-squares (max x y))
                   (max (min x y) z))

(define (max x y)
   (if (> x y) x y))

(define (min x y)
   (if (< x y) x y))
\endtt
