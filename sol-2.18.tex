\begtt\scm
(define (reverse items)
  (define (iter elements result)
    (if (null? elements)
        result
        (iter (cdr elements)
              (cons (car elements) result))))
  (iter items nil))
\endtt
